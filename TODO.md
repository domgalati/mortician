- "mortician show" should just print the selected issue, instead of duplicting "mortician list". "mortician show --render" should work identically to if an issue id was provided and should work off of "mortician select" 

- add button for timeline in webUI similar to action items. 

- clicking a field in the UI should put it into edit mode. 